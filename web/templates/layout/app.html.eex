<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="HandheldFriendly" content="true" />
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Pass the chain™</title>

    <link href="https://fonts.googleapis.com/css?family=Bungee|Bungee+Inline|Libre+Baskerville:400,400i,700|Merriweather:300,300i,400,400i,700,700i,900,900i" rel="stylesheet">
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">

    <script>window.username = "<%= assigns[:username] %>";</script>
  </head>

  <body>
    <div id="container">
      <header class="header light-blue white-text" id="page-header">
        <h1>
          <a class="white-text" href="<%= page_path(@conn, :index) %>">Awkward stories</a>
        </h1>

        <%= if @username do %>
          <section id="user-info">
            by
            <span id="username" class="text-lighten-4">
              <%= @username %>
            </span>
            and others
            <%= form_for(@conn, page_path(@conn, :logout), [id: "logout-form", as: "logout"], fn _ -> submit("(logout)") end) %>
          </section>
        <% end %>
      </header>

      <span class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></span>
      <span class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></span>

      <main role="main" class="container">
        <%= render @view_module, @view_template, assigns %>
      </main>
    </div>
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
